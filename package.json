{
  "name": "neterwala-estimation-platform",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "NODE_ENV=production ./server/node_modules/.bin/tsx server/src/index.ts",
    "setup": "npm install --prefix server && npm install --prefix client",
    "db:seed": "npx --prefix server tsx server/src/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
